version: 2
jobs:
  build:
    working_directory: ~/repo
    docker:
      - image: node:12

    steps:
      - checkout

      - run:
          name: Install dependencies
          command: npm ci

      - run:
          name: Run tests
          command: npm test

      - run:
          name: Build the app
          command: npm run build
